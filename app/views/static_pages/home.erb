<header>
  <script src="#{Rails.root}/bower_components/angular-ui-tags/build/angular-bootstrap-select.min.js"></script>

</header>

<div ng-app="sng-histogram" id="main" role="main">
  <div ng-controller='ArtistsController' ng-init="initArtists()">
    <section id="center">
      <h1>SNGram</h1>

      <p>Táto aplikácia bola vytvorená počas Hackathonu organizovaného SNG 28. a 29. novembra 2014</p>
      <a class="btn" href="http://hackathon.sng.sk/">Zistiť viac</a>
    </section>
    <section id="center">
      <img src="/assets/sng.logo.jpg" alt="sng logo">
    </section>

    <select  ng-controller="ErasController" class="selectpicker status-filter" ng-model="selectedArtist" ng-change= "viewAggregatedHistogram(selectedArtist)" >
      <option style="display:none" value="">Umelec</option>
      {{era.artist_id}}
            <option   value={{artist.id}}  ng-repeat = "artist in artists">{{artist.name}} ({{artist.records_count}})</option>
    </select>





    <ui-select ng-model="selectedArtist"
               theme="bootstrap"
               ng-disabled="disabled"
               reset-search-input="false"
               style="width: 300px;">
      <ui-select-match placeholder="Enter an address...">{{$select.selectedArtist}}</ui-select-match>
      <ui-select-choices repeat="artist in artists track by $index"
                         refresh="refreshAddresses($select.search)"
                         refresh-delay="0">
        <div ng-bind-html="artist.name "></div>
      </ui-select-choices>
    </ui-select>

    <div class="aggregated_histogram" ng-show = selectedArtist>

    </div>

  </div>
</div>

<footer id="credits">
  <p><strong>©HistroBistro</strong></p>
</footer>
